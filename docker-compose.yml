services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "3000:3000"
    volumes:
      - db-data:/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/app.db
    restart: unless-stopped

volumes:
  db-data:
